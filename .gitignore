/node_modules/
/.vs/
/.vscode/
/target/
/logs/
/dist/
# Dossier source (ancien launcher Nerysia + assets) — ne pas committer
/IMPORT/
# Fichiers NeoForge à héberger sur le FTP (générés par generate-neoforge-core.ps1)
/tools/neoforge-upload/
# distribution.json généré (servi depuis le FTP, pas besoin dans git)
/docs/distribution.json
# Log de l'installeur NeoForge (généré par generate-neoforge-core.ps1)
/installer.jar.log